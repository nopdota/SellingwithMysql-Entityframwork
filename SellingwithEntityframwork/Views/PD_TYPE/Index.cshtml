@model IEnumerable<SellingwithEntityframwork.Models.pd_type>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TP_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TP_NAME)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.TP_ID)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.TP_NAME)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.TP_ID }) |
        @Html.ActionLink("Details", "Details", new { id = item.TP_ID }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.TP_ID })
    </td>
</tr>
}

</table>
